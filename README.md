# xza (lodash mixin)

これは簡単なコードの寄せ集めです。
読み込むだけでlodashを拡張します。

```
npm install xza --save
```

```
_ = require('lodash')
require('xza')
```

## utility

- \_.sizeof( obj )
- \_.strToNumber( str, returnValue = NaN )
